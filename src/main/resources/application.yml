spring:
  application:
    name: movieflix
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/movieflix?userTimezone=true&serverTimezone=UTC
    username: root
    password: Pigmeu97
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: false
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  security:
    enabled: false
logging:
  level:
    # root: warn
    com.mybooks.library: DEBUG

